.mb-4
  %h1 Manage #{type.capitalize}s
  = link_to(admin_dashboard_path) do
    Back to admin dashboard

= turbo_frame_tag("admin-#{type}s") do
  .d-flex.gap-2.justify-content-between.align-items-center
    %h2
      Visible #{type.capitalize}s
      %span#visible-count.badge.rounded-pill.text-bg-secondary.fs-5.align-middle= visible_items.count

  .d-flex.flex-column.gap-2.mb-4{ id: "visible-items" }
    - visible_items.each do |item|
      = render("partials/admin/admin_item_card",
               item: item)

  %h2
    Hidden #{type.capitalize}s
    %span#hidden-count.badge.rounded-pill.text-bg-secondary.fs-5.align-middle= hidden_items.count
  .d-flex.flex-column.gap-2.mb-4{ id: "hidden-items" }
    - hidden_items.each do |item|
      = render("partials/admin/admin_item_card",
               item: item)

  %div{ data: { "turbo-toast": true, "notifications": flash[:notifications].to_json } }
  - flash.discard(:notifications)
