= turbo_stream.replace("new_user",
                       partial: "admin/dashboard/invite_form",
                       locals: { user: User.new, errors: nil })

= turbo_stream.append("staff-table-body",
                      partial: "admin/dashboard/staff_row",
                      locals: { user: @user.decorate })
